name: tele_web_app
description: Allows communication between your bot and the Web App built in
  Flutter displayed inside Telegram.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  js: ^0.6.4

dev_dependencies:
  coverage: ^1.1.0
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.0.0+1
